<div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">Degree Map</a>
    </div>

    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <% if (typeof nav == 'undefined') { %>
        <li><a href="/">Home</a></li>
        <li><a href="/schedule">Map</a></li>
        <li><a href="/">About</a></li>
        <% } else { %>
          <% if(nav=='Home') {%><li class="active"><%}else{%><li><%}%><a href="/">Home</a></li>
          <% if(nav=='Map') {%><li class="active"><%}else{%><li><%}%><a href="/schedule">Map</a></li>
          <% if(nav=='About') {%><li class="active"><%}else{%><li><%}%><a href="/">About</a></li>

        <% } %>
      </ul>


      <%if(typeof user == 'undefined') {%>
      <form class="navbar-form navbar-right" action="/signup">
        <button type="submit" class="btn btn-primary navbar-right">Sign up</button>
      </form>

      <p class="navbar-text navbar-right"><a href="/login" class="navbar-link">Login</a></p>

      <%} else {%>
      <form class="navbar-form navbar-right" action="/logout">
        <button type="submit" class="btn btn-primary navbar-right">Logout</button>
      </form>
      <p class="navbar-text navbar-right">Logged in as <strong><%=user.local.email%></strong></p>

      <% } %>
    </div><!--/.nav-collapse -->

  </div>
</div>
